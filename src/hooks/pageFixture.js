/**
 * This object is a middleman between the browser context and the page object for test scenarios
 * 
 * As seen below, the pageFixture object stores the current page object 
 *   that will be utilized inside of all our Page Object Models
 */

const pageFixture = {
    page:"",
}

module.exports = {pageFixture}